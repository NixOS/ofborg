[workspace]
members = [
    "ofborg",
    "ofborg-simple-build",
    "packet-exporter"
]

[profile.release]
debug = true

[patch.crates-io]
#hubcaps = { path = "../hubcaps" }
#amq-proto = { path = "rust-amq-proto" }
