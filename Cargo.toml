[workspace]
members = [
  "ofborg",
  "ofborg-send-event",
  "ofborg-simple-build",
  "hydra-evaluator",
]
resolver = "2"

[profile.release]
debug = true

[patch.crates-io]
#amq-proto = { path = "rust-amq-proto" }
